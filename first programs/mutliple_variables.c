#include <stdio.h>

int main(void) {
    int x = 5, y = 8, z = 29;
    printf("%d", x + y +z);
}

/*

Also assign the same value to 
multiple variables of the same data type:

int x, y, z;
x = y = z = 50;
printf("%d", x + y + z)
*/