#include <stdio.h>

int main(void){
    int length = 4;
    int width = 6;
    int area;

    // calculate the area of a rectangle
    area = length * width;

    printf("Length is %d\n", length);
    printf("Width is %d\n", width);
    printf("Area of the recatangle is %d\n", area);
}